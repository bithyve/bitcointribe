const INITIAL_STATE = {
    current: [],
    possible: [
        'Allie',
        'Gator',
        'Lizzie',
        'Reptar',
    ],
};

const confirmPasscode = ( state = INITIAL_STATE, action: any ) => {
    switch ( action.type ) {
        default:
            return state
    }
};

export default confirmPasscode;
import React from 'react';
import { StyleSheet, View } from 'react-native';

export type ISherpaWithGiftProps = {};

const SherpaWithGift: React.FC<ISherpaWithGiftProps> = ({ }) => {
  return (
    <View>

    </View>
  );
}

export default SherpaWithGift;

const styles = StyleSheet.create({
  
});